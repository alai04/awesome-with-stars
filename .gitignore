# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
awesome-with-stars

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Template or temporary file
/temp/
/output/
.DS_Store
